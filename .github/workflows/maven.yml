name: Retired legacy Maven workflow

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This legacy duplicate workflow is retired. Use Build DynaTech Legacy.'
